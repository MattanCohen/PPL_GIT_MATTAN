
(define make-ok
  (lambda (val)
    (cons "ok" val)
  )
 )
 
(define make-error
  (lambda (msg)
    @TODO
  )
 )

(define ok?
  (lambda (res)
    (if (= (car res) "ok") (#t) (#f))
  )
 )

(define error?
  (lambda (res)
    @TODO
  )
 )

(define result?
  (lambda (res)
    (if (ok? res) (#t) 
      (if (error? res) (#t)
       (make-error "Value is not type result in result?") 
      )
    )
 )
)

(define result->val
  (lambda (res)
    (cdr res)
  )
)

(define bind 
  (lambda (f)
    @TODO
  )
)

(define make-dict
  (lambda ()
    @TODO
  )
)

(define dict?
  (lambda (e)
    @TODO
  )
)

(define get
  (lambda (dict k)
    @TODO
  )
)

(define put
  (lambda (dict k v)
    @TODO
  )
)

(define map-dict
  (lambda (dict f)
    @TODO
  )
)

(define filter-dict
  (lambda (dict pred)
    @TODO
  )
)
